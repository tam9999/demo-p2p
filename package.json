{
  "name": "p2p-files",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.1.2"
  },
  "scripts": {
    "start": "nodemon index",
		"test": "echo \"Error: no test specified\" && exit 1",
		"server": "nodemon index",
		"heroku-postbuild": "yarn install --prefix client "
  }
}
